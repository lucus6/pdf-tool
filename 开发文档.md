# PDF 工具箱 — 开发文档

## 项目概述

基于 Python + PySide2/6 的桌面 GUI 应用，提供 PDF 拆分、合并、删除页面功能。所有操作生成新文件，不覆盖源文件。

## 技术栈

| 组件 | 选型 | 版本 | 说明 |
|------|------|------|------|
| 语言 | Python | 3.7+（开发）/ 3.11（打包） | |
| GUI | PySide2 / PySide6 | 自动适配 | 通过 `_qt_compat.py` 兼容层，优先加载 PySide6 |
| PDF 处理 | pypdf | 3.x / 6.x | 自动适配，纯 Python，MIT 协议 |
| 打包 | PyInstaller | 6.x | 使用干净 venv 构建，避免 Anaconda Qt DLL 冲突 |
| 异步 | QThread + QObject | — | Qt 内置，无需额外依赖 |

> **双 Qt 库兼容**：开发环境（Anaconda Python 3.7）使用 PySide2，打包环境（纯净 Python 3.11 venv）使用 PySide6。`_qt_compat.py` 自动检测并导入正确的库。

## 目录结构

```
PDF/
├── main.py              # 入口：QApplication 启动 + Qt DLL 修复
├── _qt_compat.py        # PySide2 / PySide6 兼容层
├── ui.py                # 所有 GUI 组件（~350 行）
│   ├── DropZone         #   可复用拖拽区域
│   ├── SplitTab         #   拆分标签页
│   ├── MergeTab         #   合并标签页
│   ├── DeleteTab        #   删除标签页
│   └── MainWindow       #   主窗口
├── engine.py            # 纯 PDF 操作逻辑（无 Qt 依赖，可独立运行测试）
├── worker.py            # QThread 异步 Worker（胶水层）
├── test_pdfs/           # 测试用 PDF 文件（不纳入版本控制）
├── build_venv/          # 干净 Python 虚拟环境（打包用，不纳入版本控制）
├── 开发文档.md
└── 使用说明书.md
```

## 架构

```
┌─────────┐     ┌──────────┐     ┌────────────┐
│  main.py │────▶│  ui.py   │────▶│  worker.py  │
│  入口     │     │  界面层   │     │  异步线程   │
└─────────┘     └────┬─────┘     └──────┬─────┘
                     │                  │
                     ▼                  ▼
               ┌──────────────────────────┐
               │       engine.py          │
               │     纯 PDF 操作逻辑       │
               │   (pypdf 调用封装)        │
               └──────────────────────────┘
```

- **engine.py** 不依赖任何 Qt 模块，可独立用 `python engine.py` 运行自测
- **_qt_compat.py** PySide2/PySide6 兼容层，其他模块通过它导入 Qt，无需关心版本
- **worker.py** 依赖兼容层，负责将 engine 调用移到后台线程
- **ui.py** 依赖兼容层 + worker + engine，负责所有交互

## engine.py API

所有公有函数的页码参数均为 **1-based**（用户习惯），内部调用 pypdf 前转换为 0-based。

```python
get_page_count(pdf_path: str) -> int
    # 返回 PDF 文件的总页数

parse_page_ranges(text: str) -> list[list[int]]
    # "1-3,5-7" → [[1,3],[5,7]]
    # 非法输入抛出 ValueError

parse_page_numbers(text: str) -> list[int]
    # "2,5,7" → [2,5,7]（自动去重、排序）
    # 非法输入抛出 ValueError

split_pdf(input_path: str, ranges: list[list[int]], output_dir: str) -> list[str]
    # 按页码范围拆分，每段生成一个文件
    # 返回输出文件路径列表
    # 输出文件名: {stem}_split_{start}_{end}.pdf

merge_pdfs(input_paths: list[str], output_path: str) -> str
    # 按顺序拼接多个 PDF
    # 返回输出文件路径

delete_pages(input_path: str, pages_to_delete: list[int], output_dir: str) -> str
    # 删除指定页，生成新文件
    # 返回输出文件路径
    # 输出文件名: {stem}_deleted.pdf
```

### 文件名冲突处理

`_unique_path(dirpath, filename)` 检查目标路径是否存在，若存在则追加 `_1`、`_2` 后缀直到不冲突。

## GUI 组件说明

### DropZone（可复用组件）

- 继承 `QLabel`，`setAcceptDrops(True)`
- 覆写 `dragEnterEvent` / `dragMoveEvent` / `dropEvent`
- 仅接受 `.pdf` 文件，拖入悬停时高亮边框
- 通过 `fileDropped(str)` 信号传递文件路径
- SplitTab 和 DeleteTab 共用此组件

### SplitTab

- 输入：PDF 文件 + 页码范围（`QLineEdit`）+ 输出目录
- 执行按钮启用条件：三者均就绪 + 范围字符串可成功解析
- 输出：每段范围生成一个 PDF

### MergeTab

- 输入：`QListWidget` 管理 PDF 列表 + 输出文件路径
- 列表支持：外部拖入、多选添加、移除、▲▼ 上移下移、`InternalMove` 拖拽排序
- 列表项格式：`文件名  —  完整路径`，解析时按 `  —  ` 分割取后半
- 执行按钮启用条件：≥2 个文件 + 输出路径已选
- 输出：单个合并 PDF

### DeleteTab

- 与 SplitTab 结构相同，页码输入格式为 `2,5,7`
- 输出：不含指定页的新 PDF

## 异步处理

所有 PDF 操作通过 `PdfWorker` + `QThread` 在后台线程执行：

```
UI 线程                          Worker 线程
────────                         ──────────
创建 QThread + PdfWorker
worker.moveToThread(thread)
thread.started → worker.run() ──▶ 执行 engine 函数
                                  │
UI 保持响应，可操作其他标签页      │
                                  │
◀── worker.finished.emit(result)  ─┘
◀── worker.error.emit(msg)        (异常时)
```

操作期间按钮禁用，操作完成后恢复。窗口关闭时 Qt 自动清理线程。

## 运行与测试

### 开发模式（Anaconda 环境）

```bash
pip install PySide2 "pypdf<4"
python main.py
```

### 运行 engine 自测

```bash
python engine.py
```

自测覆盖：`get_page_count`、`parse_page_ranges`、`parse_page_numbers`、`split_pdf`、`merge_pdfs`、`delete_pages` 以及输入校验边界情况。

### 手动集成测试

`test_pdfs/` 目录下有 3 个空白测试 PDF（10页/5页/3页），可用它们验证各功能。

## 打包为 exe

**关键**：不能在 Anaconda 环境下打包（Anaconda 自带的 Qt DLL 会污染构建）。必须使用干净 Python 环境。

### 一次性环境搭建

```bash
# 创建干净 venv（使用非 Anaconda 的 Python）
D:/MyAPP/Python/python.exe -m venv build_venv
build_venv\Scripts\python.exe -m pip install PySide6 pypdf pyinstaller
```

### 构建

```bash
build_venv\Scripts\python.exe -m PyInstaller --onefile --windowed --name "PDF工具箱" main.py
```

输出：`dist\PDF工具箱.exe`（~48MB）

### 为什么必须用干净 venv？

Anaconda 的 `Library/bin` 目录在系统 PATH 中，包含 62 个 Qt5 DLL。PyInstaller 打包时可能混入这些不匹配的 DLL，运行时 Windows 的 DLL 加载器也会优先从 PATH 中找到它们，导致 "no Qt platform plugin could be initialized" 错误。`main.py` 中有开发模式的 PATH 修复逻辑（`_fix_qt_dlls()`），但 PyInstaller 的 `--onefile` 临时解压机制使这些修复难以生效。根本解决方案是从源头隔离。

## 打包为 macOS DMG

在 **macOS** 上运行构建脚本，一键生成 `.app` 和 `.dmg`。

### 前提条件

- macOS 10.14+
- Python 3.9+（非 Anaconda，系统自带或 Homebrew 安装的均可）
- 无需安装 Xcode（hdiutil 是 macOS 内置工具）

### 构建

```bash
bash build_dmg.sh
```

脚本会自动完成：
1. 创建干净 venv
2. 安装 PySide6 + pypdf + PyInstaller
3. 构建 `PDF工具箱.app`（`--onedir` 模式，启动更快）
4. 用 `hdiutil` 打包为 `PDF工具箱.dmg`
5. 清理构建中间产物

### 输出

```
dist/
├── PDF工具箱.exe      # Windows 版
├── PDF工具箱.app/      # macOS 应用包
└── PDF工具箱.dmg       # macOS 可分发的磁盘映像
```

DMG 挂载后包含 `PDF工具箱.app` 和 `/Applications` 快捷方式，用户拖入 Applications 即完成安装。

### 可选：自定义图标

将 `icon.icns` 放到项目根目录，构建脚本会自动检测并嵌入。`.icns` 文件可用 `iconutil`（macOS 内置）从 PNG 生成。

### 与 Windows 打包的差异

| 项目 | Windows | macOS |
|------|---------|-------|
| 打包模式 | `--onefile`（单 exe） | `--onedir`（.app 包） |
| 输出格式 | .exe | .app + .dmg |
| 分发方式 | 直接发送 exe | 发送 dmg，用户拖入 Applications |
| venv Python | 独立的 Python 安装 | 系统 python3 |
| 安装体验 | 双击即用 | 挂载 DMG → 拖入 Applications → 从启动台打开 |
