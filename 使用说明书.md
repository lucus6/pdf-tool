# PDF 工具箱 — 使用说明书

## 安装

### 环境要求

- Windows / macOS / Linux
- Python 3.7 或更高版本

### 安装步骤

1. 确保已安装 Python（命令行输入 `python --version` 验证）

2. 安装依赖包：

```bash
pip install PySide2 "pypdf<4"
```

3. 下载本项目后，进入项目目录：

```bash
cd PDF
python main.py
```

## 界面概览

启动后显示主窗口，顶部有**三个标签页**，分别对应三个功能：

- **拆分 PDF** — 将一个 PDF 按指定页码范围拆成多个文件
- **合并 PDF** — 将多个 PDF 按顺序拼接成一个文件
- **删除页面** — 从 PDF 中删除指定页，生成新文件

窗口底部有状态栏，显示当前操作状态。

---

## 功能一：拆分 PDF

**适用场景**：你有一个 100 页的 PDF，只想提取第 1-3 页和第 15-20 页。

### 操作步骤

1. **选择 PDF 文件**
   - 直接将 PDF 文件拖拽到虚线框区域，或点击 **"浏览 PDF…"** 按钮选择文件
   - 选择成功后，虚线框下方会显示文件名和页数，如 `报告.pdf （100 页）`

2. **输入页码范围**
   - 在页码范围输入框中输入你想要的范围，格式为 `起始页-结束页`
   - 多个范围用**英文逗号**分隔
   - 示例：
     - `1-3` → 提取第 1 到第 3 页，生成 1 个文件
     - `1-3,5-7,10` → 生成 3 个文件（`10` 等同于 `10-10`）
     - `1,3,5` → 生成 3 个文件，各含 1 页

3. **选择输出目录**
   - 点击 **"选择输出目录…"** 按钮，选择拆分后的文件保存到哪里

4. **执行拆分**
   - 点击 **"拆分 PDF"** 按钮
   - 按钮仅在文件已选、范围格式正确、输出目录已选时才可点击
   - 处理完成后会显示生成的文件列表

### 输出文件命名

`{原文件名}_split_{起始页}_{结束页}.pdf`

例如 `报告_split_1_3.pdf`、`报告_split_5_7.pdf`。

---

## 功能二：合并 PDF

**适用场景**：你有多个独立的 PDF 文件，想按指定顺序拼成一个文件。

### 操作步骤

1. **添加 PDF 文件**
   - 将 PDF 文件直接拖入列表区域，或点击 **"添加 PDF…"** 按钮多选文件
   - 文件会按添加顺序显示在列表中

2. **调整合并顺序**（可选）
   - 选中列表中的文件，点击 **"▲ 上移"** 或 **"▼ 下移"** 调整位置
   - 也可以直接在列表内拖拽文件进行排序
   - 列表从上到下 = 合并后的页面顺序

3. **移除不需要的文件**（可选）
   - 选中文件，点击 **"移除"** 按钮

4. **选择输出位置**
   - 点击 **"选择输出位置…"**，设置合并后文件的保存路径和文件名
   - 默认文件名为 `merged.pdf`

5. **执行合并**
   - 点击 **"合并 PDF"** 按钮（至少需要 2 个文件才能点击）

---

## 功能三：删除页面

**适用场景**：你有一个 PDF，想去掉其中的空白页、广告页等。

### 操作步骤

1. **选择 PDF 文件**
   - 拖拽或浏览选择 PDF，确认显示的文件名和页数正确

2. **输入要删除的页码**
   - 输入你想删除的页码，用**英文逗号**分隔
   - 示例：
     - `1` → 删除第 1 页
     - `2,5,7` → 删除第 2、第 5、第 7 页
   - 页码可以乱序输入，系统会自动排序处理

3. **选择输出目录**
   - 点击 **"选择输出目录…"**，设置新文件的保存位置

4. **执行删除**
   - 点击 **"删除指定页"** 按钮
   - 处理完成后会显示新文件名和剩余页数

### 输出文件命名

`{原文件名}_deleted.pdf`

---

## 常见问题

**Q: 为什么拖入文件没反应？**
A: 本工具仅接受 `.pdf` 后缀的文件。请确认拖入的是 PDF 文件。

**Q: 点击按钮没反应？**
A: 请确认三项都已就绪：① PDF 文件已选择 ② 参数（范围/页码）格式正确 ③ 输出路径已选择。按钮在条件满足前保持灰色不可点击状态。

**Q: 拆分后的文件会覆盖原文件吗？**
A: 不会。所有操作都生成新文件到您指定的输出目录，源文件不会被修改或删除。

**Q: 页码输错了怎么办？**
A: 输入框会实时校验。如果格式不正确，执行按钮会保持禁用状态，直到修正为止。系统也会在点击执行时再次检查页码是否超出 PDF 总页数。

**Q: 文件很大（几百页）会卡住吗？**
A: PDF 操作在后台线程执行，界面不会卡死。处理期间您可以在状态栏看到进度提示。

**Q: 输出文件名冲突怎么办？**
A: 如果输出目录已有同名文件，系统会自动在文件名后追加 `_1`、`_2` 等数字，不会覆盖已有文件。
